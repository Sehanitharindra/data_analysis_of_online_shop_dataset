CREATE TABLE shipments(shipment_id INT,
					order_id INT,
					shipment_date DATE,
					carrier VARCHAR(20), 
					tracking_number VARCHAR(20),
					delivery_date DATE,
					shipment_status VARCHAR(20))
